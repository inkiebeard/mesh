import uuid from './libs/uuid'
// reference
class R {
  constructor (config) {
    this.id = uuid();
    this.type = 'reference'
    // todo work out map
  }

  // todo update reference node/input/output based on type
}